/**
 * tRPC types
 * Since this is a frontend-only setup, we use a minimal type definition
 * for the AppRouter. In a full-stack setup, this would be imported from the server.
 */
export type AppRouter = any;
